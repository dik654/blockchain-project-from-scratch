[package]
name = "zk-vm"
version = "0.1.0"
edition.workspace = true

[dependencies]
# STARK from scratch — arkworks 의존성 없음
# 유한체, 다항식, FRI, AIR, STARK prover/verifier 전부 직접 구현
sha2.workspace = true
rand.workspace = true
rand_chacha.workspace = true
